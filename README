# Tripistry — Travel Package Comparison Platform

COS 221 Practical Assignment 5 | University of Pretoria | 2026

---

## Overview

Tripistry is a full-stack web application that allows travellers to browse, compare and book
travel packages from multiple verified travel agencies. Travel agencies can create and manage
packages, view bookings and read customer reviews through a dedicated agency dashboard.

The application is built with PHP (OOP), MariaDB and vanilla JavaScript, and is served
through Apache via XAMPP.

---

## Team

| Student Number | Name               | Role                                                        |
|----------------|--------------------|-------------------------------------------------------------|
| u24876489      | Katlego Sentsho    | API development, Reviews, Favourites, DB schema, Agency UI  |
| u25042239      | Tebogo Mokgwatsane | API development, Login & SignUp, Traveller Browsing         |
| u24589137      | [Manqoba Mashego]    | [API development, Traveller UI, merging of files]                                                      |
| u25591267      | [Ntando Dlamini]    | [API development, EER, Relational schema, Pyments, Bookings]    
| u23535513      | Cris Rankoko        | API development, Traveller Browsing                                             |

---

## Tech Stack

| Layer      | Technology                        |
|------------|-----------------------------------|
| Backend    | PHP 8.x (OOP, REST API)           |
| Database   | MariaDB 12.2                      |
| Frontend   | HTML5, CSS3, Vanilla JavaScript   |
| Server     | Apache via XAMPP                  |
| Version Control | Git + GitHub                 |

---

## Prerequisites

Before running the application, ensure the following are installed:

- [XAMPP](https://www.apachefriends.org/) (for Apache + PHP)
- [MariaDB 12.2](https://mariadb.org/download/) (standalone, separate from XAMPP MySQL)
- A modern web browser (Chrome, Firefox, Edge)
- Git

> **Important:** XAMPP's built-in MySQL must be **stopped** in the XAMPP Control Panel.
> Only Apache should be running from XAMPP. MariaDB runs as a separate Windows service.

---

## Installation and Setup

### Step 1 — Clone the Repository


git clone https://github.com/[your-repo-url]/COS221-Tripistry.git


Or extract the submitted ZIP archive into your XAMPP web root:


C:\xampp\htdocs\COS221-Tripistry\


### Step 2 — Create the Database

Open a terminal and run:


"C:\Program Files\MariaDB 12.2\bin\mysql.exe" -u root -p -e "CREATE DATABASE IF NOT EXISTS tripistry;"


### Step 3 — Import the Schema


"C:\Program Files\MariaDB 12.2\bin\mysql.exe" -u root -p tripistry < "sql/create_tables.sql"


### Step 4 — Populate the Data


"C:\Program Files\MariaDB 12.2\bin\mysql.exe" -u root -p tripistry < "sql/populate_data.sql"


### Step 5 — Configure the Database Connection

Open `Tripistry/config.php` and update the password to match your MariaDB root password:


$mysqli = new mysqli('127.0.0.1', 'root', 'YOUR_PASSWORD', 'tripistry', 3306);

If you have no password set, leave it as an empty string `''`.

### Step 6 — Start Apache

Open the XAMPP Control Panel and click **Start** next to **Apache**.
Ensure MariaDB is running as a Windows service (check Services via Win+R → `services.msc`).

### Step 7 — Open the Application

Navigate to:


http://localhost/COS221-Tripistry/Tripistry/


---

## Folder Structure

COS221-Tripistry/
├── api.php                  ← REST API entry point (all endpoints)
├── Tripistry/
│   ├── config.php           ← Database connection
│   ├── auth.php             ← Role-based access control guard
│   ├── index.php            ← Landing page
│   ├── login.html           ← Login page
│   ├── signup.html          ← Registration page
│   ├── css/                 ← Global stylesheets
│   ├── scripts/             ← Global JavaScript
│   └── img/                 ← Icons and images
├── traveller/
│   ├── index.php            ← Package browsing dashboard
│   ├── package.php          ← Single package detail view
│   ├── reviews.php          ← Booking history and reviews
│   ├── favourites.php       ← Saved packages
│   ├── navbar.php           ← Traveller navigation
│   ├── css/                 ← Traveller-specific styles
│   └── scripts/             ← Traveller-specific JavaScript
├── agency/
│   ├── index.php            ← Agency dashboard
│   ├── packages.php         ← Package management
│   ├── bookings.php         ← Booking overview
│   ├── reviews.php          ← Customer reviews
│   ├── navbar.php           ← Agency navigation
│   ├── css/                 ← Agency-specific styles
│   └── scripts/             ← Agency-specific JavaScript
└── sql/
    ├── create_database.sql  ← Creates the tripistry database
    ├── create_tables.sql    ← All table definitions and constraints
    └── populate_data.sql    ← All seed data inserts



## API Reference

The REST API is located at `http://localhost/COS221-Tripistry/api.php`.

All requests use `POST` with a JSON body containing at minimum a `type` field.

### Public Endpoints (no authentication required)

| Type | Parameters | Description |
|------|-----------|-------------|
| `Register` | `username`, `email`, `password`, `user_type`, `agency_name`*, `registration_number`* | Register a new user |
| `Login` | `email`, `password` | Log in and receive an API key |
| `GetPackages` | `dest_id`?, `min_price`?, `max_price`?, `search`?, `sort_by`?, `order`? | Browse all active packages |
| `GetPackage` | `package_id` | Get full details for one package |
| `GetReviews` | `package_id` | Get all reviews for a package |

### Traveller Endpoints (api_key required)

| Type | Parameters | Description |
|------|-----------|-------------|
| `GetBookings` | `api_key` | Get all bookings for the logged-in traveller |
| `AddReview` | `api_key`, `package_id`, `rating`, `comment`? | Submit a review |
| `AddFavourite` | `api_key`, `package_id` | Save a package to favourites |
| `RemoveFavourite` | `api_key`, `package_id` | Remove a package from favourites |
| `GetFavourites` | `api_key` | Get all favourited packages |

### Agency Endpoints (api_key required, travel_agent only)

| Type | Parameters | Description |
|------|-----------|-------------|
| `GetAgencyPackages` | `api_key` | Get all packages for the logged-in agency |
| `GetAgencyBookings` | `api_key` | Get all bookings for agency packages |
| `GetAgencyReviews` | `api_key` | Get all reviews for agency packages |
| `CreatePackage` | `api_key`, `title`, `dest_id`, `price`, `quantity`, `expiry_date`, `status`, `description`?, `is_group`?, `min_group_size`?, `max_group_size`? | Create a new package |
| `UpdatePackage` | `api_key`, `package_id`, + same as CreatePackage | Edit an existing package |
| `DeletePackage` | `api_key`, `package_id` | Delete a package |

`*` Required for agency registration only
`?` Optional parameter

---

## Test Credentials

| Email | Password | Role |
|-------|----------|------|
| `info@sunwaytravels.co.za` | `agent123` | Travel Agent |
| `hello@globehopper.co.za` | `agent456` | Travel Agent |
| `katlego@mail.com` | `pass123` | Traveller |
| `thabo@mail.com` | `pass456` | Traveller |
| `lerato@mail.com` | `pass789` | Traveller |

---

## Data Validation

### Client-Side (JavaScript)
- Email format validated using regex before form submission
- Password requirements enforced: minimum 8 characters
- Required fields checked before any API call is made
- Package form validates price > 0, quantity ≥ 0 and expiry date is set
- Star rating must be selected before review submission

### Server-Side (PHP)
- All inputs sanitised and type-cast before use in SQL queries
- Prepared statements with `bind_param` used throughout — no raw SQL string concatenation
- API key validated against database on every protected endpoint
- User role (`user_type`) verified server-side for every agency and traveller endpoint
- Business registration number checked against `businessregistration` table before agency signup
- Duplicate email checked before user registration
- Review submission validates that a confirmed booking exists for the traveller and package

---

## Role-Based Access Control

Access to pages is enforced at the PHP level using `auth.php`:


require_once '../Tripistry/auth.php';
requireRole('travel_agent'); // or 'traveller'


- A traveller visiting an agency page is redirected to the traveller dashboard
- An agency visiting a traveller page is redirected to the agency dashboard
- Any unauthenticated user is redirected to the login page
- This check occurs before any HTML is rendered, preventing partial page exposure

---

## Known Limitations

- Images (`img_url`) for packages, accommodation and attractions are stored as URL references.
  No actual image upload functionality is implemented; placeholder icons are used throughout.
- The application is configured for local development. To deploy to a remote server, update
  all `http://localhost/COS221-Tripistry/` references in JavaScript files to the server URL.
- Password hashing uses SHA-256 (via PHP's `hash()` function) for compatibility with the
  existing dataset. bcrypt (`password_hash`) would be preferred in a production environment.

---

## Git Workflow

The team used a feature-branch workflow:

- `main` — stable, submission-ready code only
- `dev` — integration branch for merging feature branches
- `feature/[name]` — individual feature branches (e.g. `feature/reviews`, `feature/agency-dashboard`)

Commit message format:

[type]: short description

types: feat, fix, refactor, docs, style, chore


Examples:

feat: add GetBookings endpoint with already_reviewed flag
fix: resolve variable scope clash between reviews.js and navbar.js
docs: update README with installation steps
refactor: wrap all traveller JS files in IIFE to prevent global clashes


---

## SQL Files

Located in the `sql/` folder:

| File | Contents |
|------|----------|
| `create_database.sql` | `CREATE DATABASE IF NOT EXISTS tripistry` |
| `create_tables.sql` | All `CREATE TABLE` statements with constraints and indexes |
| `populate_data.sql` | All `INSERT INTO` statements for seed data |

To reimport from scratch:

"C:\Program Files\MariaDB 12.2\bin\mysql.exe" -u root -p -e "DROP DATABASE IF EXISTS tripistry; CREATE DATABASE tripistry;"
"C:\Program Files\MariaDB 12.2\bin\mysql.exe" -u root -p tripistry < sql/create_tables.sql
"C:\Program Files\MariaDB 12.2\bin\mysql.exe" -u root -p tripistry < sql/populate_data.sql



